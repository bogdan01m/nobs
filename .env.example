# NOBS Benchmark Configuration

# HuggingFace Settings
HF_HUB_ENABLE_HF_TRANSFER=1  # Speeds up model downloads

# ============================================================
# Backend Selection
# ============================================================
# Options: AUTO, LM_STUDIO, OLLAMA, or BOTH
#
# AUTO - tries LM Studio first, falls back to Ollama (uses one)
# LM_STUDIO - forces LM Studio backend only
# OLLAMA - forces Ollama backend only
# BOTH - runs benchmarks on BOTH LM Studio AND Ollama for comparison
#
# Note: BOTH mode will run each benchmark twice (once per backend)
# and save results for both in the same report

LLM_BACKEND=BOTH
VLM_BACKEND=BOTH

# ============================================================
# LM Studio Settings (default port: 1234)
# ============================================================

LLM_API_KEY=api-key
LLM_BASE_URL=http://127.0.0.1:1234/v1
LLM_MODEL_NAME=gpt-oss-20b

# VLM Settings (falls back to LLM settings if not specified)
VLM_API_KEY=api-key
VLM_BASE_URL=http://127.0.0.1:1234/v1
VLM_MODEL_NAME=qwen/qwen3-vl-8b

# ============================================================
# Ollama Settings (default port: 11434)
# ============================================================

OLLAMA_LLM_BASE_URL=http://127.0.0.1:11434/v1
OLLAMA_LLM_MODEL_NAME=gpt-oss:20b

# Ollama VLM Settings
OLLAMA_VLM_BASE_URL=http://127.0.0.1:11434/v1
OLLAMA_VLM_MODEL_NAME=qwen3-vl:8b

# ============================================================
# Usage Examples
# ============================================================

# Example 1: Run on BOTH backends for comparison (DEFAULT - recommended)
# LLM_BACKEND=BOTH
# VLM_BACKEND=BOTH

# Example 2: Use LM Studio for everything
# LLM_BACKEND=LM_STUDIO
# VLM_BACKEND=LM_STUDIO

# Example 3: Use Ollama for everything
# LLM_BACKEND=OLLAMA
# VLM_BACKEND=OLLAMA

# Example 4: Use LM Studio for LLM, Ollama for VLM (mixed)
# LLM_BACKEND=LM_STUDIO
# VLM_BACKEND=OLLAMA

# Example 5: Auto-detect (tries LM Studio first, then Ollama as fallback)
# LLM_BACKEND=AUTO
# VLM_BACKEND=AUTO
